import React, {ReactElement} from 'react';
import {Header} from 'semantic-ui-react';

export default function Round(): ReactElement {
  return <section>
    <Header as='h4'>Spieler</Header>
  </section>;
}
